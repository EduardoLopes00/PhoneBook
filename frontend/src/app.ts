import { styled } from "styled-components";
export const Container = styled.div`
  min-height: 100vh;
  min-width: 100vw;
  display: flex;

  overflow-x: hidden;
  overflow-y: hidden;

  flex-grow: 1;
`;
