import styled from "styled-components";
import { mainOrange } from "../../styles/tokens";

export const StyledButton = styled.button`
  width: 100px;
  height: 25px;
  border: none;
  background-color: ${mainOrange};

  border-radius: 3px;
  color: black;
  cursor: pointer;
`;
